using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TourEdition : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PlayerMove_Tour.istouring = true;
    }
}
